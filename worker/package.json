{
  "name": "pshatgpt-proxy",
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for PshatGPT — free-tier explanation streaming",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
